{
  "version": "1.16.2",
  "notes": [
    "Account sync now safely handles Cloudflare receipts and avoids overlapping expense syncs",
    "Local receipt cache entries now track their matching R2 object key",
    "Replacing a receipt invalidates the older image on other devices",
    "The latest cloud receipt downloads automatically when the cached version is stale"
  ]
}
