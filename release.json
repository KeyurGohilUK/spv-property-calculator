{
  "version": "1.11.0",
  "notes": [
    "Prevents an older device from silently overwriting a newer property edit",
    "Keeps unsynced local changes safe when a cloud conflict is detected",
    "Uses server-controlled revisions instead of device clocks for successful writes",
    "Adds automated conflict and revision tests"
  ]
}
