{
  "version": "1.13.0",
  "notes": [
    "Expense filtering and CSV export are now combined in one panel",
    "The main action is renamed Filter & Export",
    "CSV export continues to use the active filters",
    "The separate report-card export button has been removed"
  ]
}
