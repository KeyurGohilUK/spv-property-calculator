{
  "version": "1.12.1",
  "notes": [
    "Expense records now sync across signed-in workspace devices",
    "Offline expense changes sync automatically when connectivity returns",
    "Conflict-safe revisions preserve local changes for review",
    "Receipt files remain stored only on the device where they were uploaded"
  ]
}
