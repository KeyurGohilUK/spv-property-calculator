{
  "version": "1.14.5",
  "notes": [
    "Properties and Expenses now use one shared sync-status component",
    "Offline status uses the same neutral styling throughout the app",
    "Only genuine sync failures display in red",
    "Successful sync status remains green"
  ]
}
