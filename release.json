{
  "version": "1.15.0",
  "notes": [
    "Private Cloudflare R2 receipt-service infrastructure is ready",
    "Worker requests validate Supabase users and workspace permissions",
    "Receipt binaries remain private while Supabase stores only object keys",
    "Browser integration will be enabled after Worker deployment"
  ]
}
