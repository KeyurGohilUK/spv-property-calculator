{
  "version": "1.16.0",
  "notes": [
    "Optimised receipts now upload securely to the private Cloudflare R2 bucket",
    "Supabase stores the private object key so receipts work across signed-in devices",
    "Receipt viewing prefers the offline copy and downloads from R2 when needed",
    "Failed or offline uploads remain local and retry through Sync now"
  ]
}
